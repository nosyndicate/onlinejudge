#include<cstdio>
using namespace std;

int matrix[101][101];

int main()
{
    int row, column;
    scanf("%d %d",&row,&column);
    for(int i = 0;i<row;++i)
    {
	for(int j = 0;j<column;++j)
	{
	    scanf("%d",&matrix[i][j]);
	}
    }

    for(int i = 0;i<row;++i)
    {
	for(int j = 0;j<column;++j)
	{
	    printf("%d",matrix[i][j]);
	}
    }
    return 0;
}
